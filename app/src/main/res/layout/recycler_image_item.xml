<?xml version="1.0" encoding="utf-8"?>
<com.example.luke_imagevideo_send.http.views.SquareRelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:padding="5dp"
    android:gravity="center_vertical">

    <ImageView
        android:id="@+id/imageView"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:scaleType="fitXY"
        tools:src="@color/app_color_f6" />

    <ImageView
        android:id="@+id/ivDel"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignTop="@id/imageView"
        android:layout_alignRight="@id/imageView"
        android:scaleType="centerInside"
        android:src="@drawable/ic_delete_menu"
        android:visibility="gone"
        tools:visibility="visible" />

    <TextView
        android:id="@+id/tvLong"
        android:layout_width="match_parent"
        android:layout_height="30dp"
        android:layout_alignLeft="@id/imageView"
        android:layout_alignRight="@id/imageView"
        android:layout_alignBottom="@id/imageView"
        android:background="@drawable/picture_icon_shadow_bg"
        android:drawableLeft="@drawable/picture_icon_video"
        android:drawablePadding="5dp"
        android:gravity="center_vertical"
        android:paddingLeft="5dp"
        android:paddingTop="8dp"
        android:text="00:00"
        android:textColor="@color/app_color_white"
        android:textSize="11sp"
        android:visibility="gone"
        tools:visibility="visible" />
</com.example.luke_imagevideo_send.http.views.SquareRelativeLayout>